import React from 'react'
import Icon from 'react-native-vector-icons/Ionicons';

const Ionicons = ({name,color,size}) => {
  return (
    <Icon name={name} color={color} size={size}/>
  )
}

export default Ionicons